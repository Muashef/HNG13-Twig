<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Ticket Management{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/twig/public/css/style.css">
    <style>
        @keyframes wave {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }
        .wave-animation {
            animation: wave 3s ease-in-out infinite;
        }
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        .fade-in-up {
            animation: fadeInUp 0.6s ease-out;
        }
    </style>
</head>
<body class="bg-slate-950 text-slate-100 font-sans">
    <nav class="bg-slate-900 border-b border-slate-800 sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <a href="/twig/public/" class="text-2xl font-bold bg-gradient-to-r from-cyan-400 to-blue-500 bg-clip-text text-transparent">
                    TicketZen
                </a>
                <div class="flex gap-4">
                    {% if page == 'dashboard' %}
                        <button onclick="handleLogout()" class="px-4 py-2 bg-red-600 hover:bg-red-700 rounded-lg transition">
                            Logout
                        </button>
                    {% else %}
                        <a href="/twig/public/login" class="px-4 py-2 text-slate-300 hover:text-white transition">Login</a>
                        <a href="/twig/public/signup" class="px-4 py-2 bg-cyan-600 hover:bg-cyan-700 rounded-lg transition">Sign Up</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>

    {% block content %}{% endblock %}

    <script src="/twig/public/js/storage.js"></script>
    <script src="/twig/public/js/validation.js"></script>
    <script src="/twig/public/js/app.js"></script>
</body>
</html>
